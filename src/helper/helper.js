export function capitalize(str) {
    const capitalized = str.charAt(0).toUpperCase() + str.slice(1);

    return capitalized;
}

// yoinked from https://www.programiz.com/javascript/examples/first-letter-uppercase
